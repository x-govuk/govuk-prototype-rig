import express from 'express'

const router = express.Router()

/**
 * Add your routes below this comment block.
 *
 * @example
 * router.get('/examples/:view', (req, res) => {
 *   const { view } = req.params
 *   res.render(view)
 * })
 */

export default router
